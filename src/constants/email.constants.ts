import { EEmailActions } from "../eums/email.enum";

export const allTemplates = {
  [EEmailActions.WELLCOME]: {
    templateName: "register",
    subject: "Hello! Wellcome to our platform!",
  },
};
